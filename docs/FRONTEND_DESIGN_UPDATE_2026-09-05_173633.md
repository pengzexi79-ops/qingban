# 前端设计更新记录

- 更新时间：2026-09-05 17:36:33 +08:00（Asia/Shanghai）
- 开发分支：`dev-lwtpxr-frontend-design`
- 基于主干：`origin/master` 最新提交 `490a028`
- 负责范围：前端启动页、会话流程和“我的”页面信息架构

## 本次更新

- 新增亲伴启动加载页：现实与记忆中的两个人物相互张望，中间用流动光点表达连接，约 1.8 秒后淡出进入首页。
- 保留最初确认的双气泡品牌图标，并补充本地 `frontend/assets/qinban-icon.svg` 资源。
- 会话页移除“全部 / 未读 / 单聊 / 群聊”筛选条，保留搜索和会话列表。
- 未读状态改为显示在具体会话旁的红点，不再显示顶部数字。
- 会话和底部导航使用更清晰的聊天气泡图标。
- 朋友圈页移除“AI 主动朋友圈”控制区，相关开关和模拟入口移动到“我的 → 我的设置”。
- “我的”页面移除 API 损耗、个人与产品、后端数据台和本地数据等技术化分组，只保留收藏、AI 群聊与角色、设置中心、主动朋友圈、个人资料和隐私说明。

## 未纳入本次更新

- 未确认的桥梁、叶片、星点、丝线、结点等图标草稿未加入分支。
- 未修改后端、接口契约或其他成员的文件。
- 未直接修改 `master`，未使用 force push。

## 验证记录

- `node --check frontend/js/app.js` 通过。
- `git diff --check` 通过。
- 远程 `master` 已通过普通 merge 合入当前分支，当前分支包含 `origin/master` 最新提交且无冲突。
- 本地独立预览：`http://localhost:8768/`。

## 合作说明

本次更新应通过 Pull Request 审核，由审核人确认后使用 Squash and merge 合并；本分支不直接推送 `master`。
